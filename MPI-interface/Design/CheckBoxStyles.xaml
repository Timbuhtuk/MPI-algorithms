<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="CheckBox.Sonar" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}" />
        <Setter Property="FontFamily" Value="{DynamicResource FontFamily.Base}" />
        <Setter Property="FontSize" Value="{DynamicResource FontSize.Body}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Margin" Value="0 0 10 10" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Border x:Name="RootBorder"
                            Background="{DynamicResource Brush.Input.Background}"
                            BorderBrush="{DynamicResource Brush.Input.Border}"
                            BorderThickness="1"
                            CornerRadius="{DynamicResource Radius.Medium}"
                            Padding="12 10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20" />
                                <ColumnDefinition Width="8" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border x:Name="CheckBorder"
                                    Grid.Column="0"
                                    Width="18"
                                    Height="18"
                                    CornerRadius="6"
                                    Background="Transparent"
                                    BorderBrush="{DynamicResource Brush.Input.Border}"
                                    BorderThickness="1.5"
                                    VerticalAlignment="Center">
                                <Path x:Name="CheckIcon"
                                      Data="M 3 9 L 7 13 L 15 5"
                                      Stroke="{DynamicResource Brush.Text.Dark}"
                                      StrokeThickness="2"
                                      StrokeStartLineCap="Round"
                                      StrokeEndLineCap="Round"
                                      StrokeLineJoin="Round"
                                      Visibility="Collapsed" />
                            </Border>
                            <ContentPresenter Grid.Column="2"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Brush.TabBar.HoverBackground}" />
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Brush.Input.BorderFocus}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckBorder" Property="Background" Value="{DynamicResource Brush.Text.Dark}" />
                            <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{DynamicResource Brush.Text.Dark}" />
                            <Setter TargetName="CheckIcon" Property="Visibility" Value="Visible" />
                            <Setter TargetName="CheckIcon" Property="Stroke" Value="{DynamicResource Brush.Text.Primary}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="RootBorder" Property="Opacity" Value="0.5" />
                            <Setter Property="Foreground" Value="{DynamicResource Brush.Input.DisabledForeground}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CheckBox.Sonar.Compact"
           TargetType="CheckBox"
           BasedOn="{StaticResource CheckBox.Sonar}">
        <Setter Property="Margin" Value="0 0 8 8" />
        <Setter Property="FontSize" Value="{DynamicResource FontSize.Caption}" />
    </Style>
</ResourceDictionary>
