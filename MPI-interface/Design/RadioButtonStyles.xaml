<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="RadioButton.Sonar" TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}"/>
        <Setter Property="FontFamily" Value="{DynamicResource FontFamily.Base}"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSize.Body}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="RootBorder"
                            Background="{DynamicResource Brush.Input.Background}"
                            BorderBrush="{DynamicResource Brush.Input.Border}"
                            BorderThickness="1"
                            CornerRadius="{DynamicResource Radius.Medium}"
                            Padding="12 10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- circle -->
                            <Border x:Name="OuterCircle"
                                    Grid.Column="0"
                                    Width="18"
                                    Height="18"
                                    CornerRadius="9"
                                    Background="Transparent"
                                    BorderBrush="{DynamicResource Brush.Input.Foreground}"
                                    BorderThickness="1.5"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Center">
                                <Ellipse x:Name="InnerDot"
                                         Width="8"
                                         Height="8"
                                         Fill="{DynamicResource Brush.Input.Foreground}"
                                         Visibility="Collapsed"/>
                            </Border>

                            <!-- content -->
                            <ContentPresenter Grid.Column="2"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              RecognizesAccessKey="True"/>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Brush.TabBar.HoverBackground}"/>
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Brush.Input.Foreground}"/>
                        </Trigger>

                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="OuterCircle" Property="BorderBrush" Value="{DynamicResource Brush.Input.Foreground}"/>
                            <Setter TargetName="InnerDot" Property="Visibility" Value="Visible"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="RootBorder" Property="Opacity" Value="0.5"/>
                            <Setter Property="Foreground" Value="{DynamicResource Brush.Input.DisabledForeground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="RadioButton.Sonar.Compact"
           TargetType="RadioButton"
           BasedOn="{StaticResource RadioButton.Sonar}">
        <Setter Property="Margin" Value="0"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSize.Caption}"/>
        <Setter Property="Padding" Value="10 8"/>
    </Style>

</ResourceDictionary>

